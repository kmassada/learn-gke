apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  annotations:
    volume.beta.kubernetes.io/storage-class: ssd
  name: claim2
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 30Gi